{
	"scripts": {
		"build": "bun build --minify --splitting --outdir=./scroll-snap-slider/scroll-slider/build ./scroll-snap-slider/scroll-slider/scroll-slider.js"
	},
	"dependencies": {
		"gsap": "^3.14.2"
	}
}
